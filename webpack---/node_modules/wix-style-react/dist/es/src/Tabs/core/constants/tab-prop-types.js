import PropTypes from 'prop-types';
import TabTypes from './tab-types';
var stringOrNumber = PropTypes.oneOfType([PropTypes.string, PropTypes.number]);
export var item = PropTypes.shape({
  id: stringOrNumber,
  title: PropTypes.node,
  dataHook: PropTypes.string
});
export var items = PropTypes.arrayOf(item);
export var onClick = PropTypes.func;
export var activeId = stringOrNumber;
export var width = stringOrNumber;
export var sideContent = PropTypes.node;
export var type = PropTypes.oneOf(TabTypes);