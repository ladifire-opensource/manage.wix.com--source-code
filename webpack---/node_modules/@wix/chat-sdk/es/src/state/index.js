import { rootReducer } from './reducer/reducers.index';
import * as actions from './action/action.index';
import setupStoreCommon from './setupStore';
import types from './types';
var setupStore = function (_a) {
    var initialState = (_a === void 0 ? {} : _a).initialState;
    return setupStoreCommon(rootReducer, initialState);
};
export { setupStore, actions, rootReducer, types };
