import { DialogNames } from '../../../types';
export var getRequestedDialogFromUrl = function () {
    var requestedDialog = (new URLSearchParams(window.location.search)).get('action');
    if (requestedDialog && Object.values(DialogNames).includes(requestedDialog)) {
        return requestedDialog;
    }
};
//# sourceMappingURL=get-requested-dialog-from-url.js.map