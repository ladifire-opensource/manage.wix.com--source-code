import * as React from 'react';
import FolderMoveSmall from 'wix-ui-icons-common/FolderMoveSmall';
import { MoveToFolderSiteAction } from './MoveToFolderSiteAction';
import { DialogNames, ActionType } from '../../../../types';
import { PermissionMethods } from '../../../../services/actionsPermissions';
import { PERMISSIONS } from '../../../../consts/permissions';
export var getMoveToFolderSiteAction = function (quickActionsData) {
    var isInFolder = Boolean(quickActionsData.foldersInfo);
    return {
        type: ActionType.dialog,
        dialog: {
            dialogName: DialogNames.moveSiteToFolder,
            component: MoveToFolderSiteAction,
            biName: 'quick_actions.move_to_folder'
        },
        actionName: 'move-to-folder',
        textKey: 'quickActions.move_to_folder',
        icon: React.createElement(FolderMoveSmall, null),
        visible: isInFolder,
        isPermitted: PermissionMethods.withPermissions(PERMISSIONS.MOVE_SITE_TO_FOLDER),
    };
};
//# sourceMappingURL=getMoveToFolderSiteAction.js.map