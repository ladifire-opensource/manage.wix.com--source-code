import { getSiteActionDefinitions } from '../SiteActions';
import { getActionFromDefinition } from './getActionFromDefinition';
import { getLoaderSiteAction } from '../SiteActions/Loader/getLoaderSiteAction';
export function getSiteActions(_a) {
    var quickActionsData = _a.quickActionsData, experiments = _a.experiments, dealerData = _a.dealerData, openDialog = _a.openDialog, origin = _a.origin, t = _a.t, ready = _a.ready, onDialogActionComplete = _a.onDialogActionComplete;
    var siteActions = !ready ?
        [getLoaderSiteAction()] :
        getSiteActionDefinitions({ quickActionsData: quickActionsData, origin: origin, experiments: experiments, dealerData: dealerData });
    var actionExtraction = getActionFromDefinition({ quickActionsData: quickActionsData, openDialog: openDialog, origin: origin, t: t, onDialogActionComplete: onDialogActionComplete });
    return siteActions.map(actionExtraction);
}
//# sourceMappingURL=getSiteActions.js.map