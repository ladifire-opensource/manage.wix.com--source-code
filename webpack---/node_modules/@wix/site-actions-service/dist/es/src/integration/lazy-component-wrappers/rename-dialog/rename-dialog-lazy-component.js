import { __extends } from "tslib";
import { ModuleRegistry, ReactLazyComponent } from 'react-module-container';
import { RENAME_DIALOG_COMPONENT_NAME, RENAME_DIALOG_LAZY_COMPONENT_NAME, DialogBundleNames } from '../config';
var minified = function (debug) { return (debug ? '' : '.min'); };
var RenameDialogLazyComponent = /** @class */ (function (_super) {
    __extends(RenameDialogLazyComponent, _super);
    function RenameDialogLazyComponent(props) {
        return _super.call(this, props, {
            files: [
                "" + props.siteActionsStaticsUrl + DialogBundleNames.Rename + ".bundle" + minified(props.debug) + ".js",
                "" + props.siteActionsStaticsUrl + DialogBundleNames.Rename + minified(props.debug) + ".css",
            ],
            component: RENAME_DIALOG_COMPONENT_NAME,
        }) || this;
    }
    return RenameDialogLazyComponent;
}(ReactLazyComponent));
export { RenameDialogLazyComponent };
ModuleRegistry.registerComponent(RENAME_DIALOG_LAZY_COMPONENT_NAME, function () { return RenameDialogLazyComponent; });
//# sourceMappingURL=rename-dialog-lazy-component.js.map