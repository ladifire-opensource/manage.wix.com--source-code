import i18next from 'i18next';
import resources from './resources';
var _instance;
export default function getTranslator(lang) {
    if (!_instance) {
        _instance = i18next.createInstance();
        _instance.init({
            lng: 'en',
            fallbackLng: 'en',
            keySeparator: false,
            resources: resources,
        });
    }
    return _instance.getFixedT(lang);
}
//# sourceMappingURL=index.js.map