'use strict';

exports.__esModule = true;

var _env = require('../env');

/**
 * Simple GET request wrapper
 */
exports.default = {
  fetch: function fetch(url) {
    var Image = (0, _env.env)().Image;
    new Image().src = url;
  }
};