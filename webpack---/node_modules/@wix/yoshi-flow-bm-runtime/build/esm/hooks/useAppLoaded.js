import { useCallback, useEffect } from 'react';
import { notifyViewFinishedLoading } from '@wix/business-manager-api';
import { useFedops } from '../fedops';
import useComponentInfo from './useComponentInfo';
var useAppLoaded = function (_a) {
    var _b = _a === void 0 ? {} : _a, auto = _b.auto;
    var componentId = useComponentInfo().componentId;
    var fedops = useFedops();
    var callback = useCallback(function () {
        notifyViewFinishedLoading(componentId);
        if (fedops) {
            fedops.appLoaded();
        }
    }, [componentId, fedops]);
    useEffect(function () {
        if (auto) {
            callback();
        }
    }, [callback]);
    return callback;
};
export default useAppLoaded;
//# sourceMappingURL=useAppLoaded.js.map