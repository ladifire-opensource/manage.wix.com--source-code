import React, { createContext } from 'react';
export const SentryContext = createContext(null);
const SentryProvider = ({ client, children }) => {
    return (React.createElement(SentryContext.Provider, { value: client }, children));
};
export default SentryProvider;
//# sourceMappingURL=SentryProvider.js.map