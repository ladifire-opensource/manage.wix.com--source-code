import React, { createContext, useMemo } from 'react';
import { notifyViewStartLoading } from '@wix/business-manager-api';
export const ComponentContext = createContext(undefined);
const ComponentInfoProvider = ({ componentInfo, children, }) => {
    useMemo(() => {
        if (componentInfo.type === 'page') {
            notifyViewStartLoading(componentInfo.componentId);
        }
    }, []);
    return (React.createElement(ComponentContext.Provider, { value: componentInfo }, children));
};
export default ComponentInfoProvider;
//# sourceMappingURL=ComponentInfoProvider.js.map