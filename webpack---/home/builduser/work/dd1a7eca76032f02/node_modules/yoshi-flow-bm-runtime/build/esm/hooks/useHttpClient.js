import { HttpContext } from 'yoshi-serverless-react';
import { useContext } from 'react';
export default function useHttpClient() {
    const { client } = useContext(HttpContext);
    if (client == null) {
        throw new Error('`useHttpClient` was called with illegal value provided to HttpProvider');
    }
    return client;
}
//# sourceMappingURL=useHttpClient.js.map