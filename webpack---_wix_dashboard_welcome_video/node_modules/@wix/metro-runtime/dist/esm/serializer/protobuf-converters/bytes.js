var _a;
import { ConverterType } from '../domain';
export var bytes = (_a = {
        types: ['google.protobuf.BytesValue', 'BYTES']
    },
    _a[ConverterType.TO_JSON] = {
        transfrom: function (val) {
            var chars = val.reduce(function (res, c) { return res + String.fromCharCode(c); }, '');
            return btoa(chars);
        },
    },
    _a[ConverterType.FROM_JSON] = {
        transfrom: function (val) {
            return Uint8Array.from(atob(val), function (c) { return c.charCodeAt(0); });
        },
    },
    _a);
//# sourceMappingURL=bytes.js.map