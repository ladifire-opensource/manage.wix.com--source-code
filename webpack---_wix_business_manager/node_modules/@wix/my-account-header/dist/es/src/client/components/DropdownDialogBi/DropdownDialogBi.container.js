import { connect } from 'react-redux';
import { getCurrentSiteId } from '../../selectors/currentSiteSelectors';
import { DropdownDialogBi } from './DropdownDialogBi';
function mapStateToProps(state) {
    return {
        metaSiteId: getCurrentSiteId(state)
    };
}
export default connect(mapStateToProps)(DropdownDialogBi);
//# sourceMappingURL=DropdownDialogBi.container.js.map