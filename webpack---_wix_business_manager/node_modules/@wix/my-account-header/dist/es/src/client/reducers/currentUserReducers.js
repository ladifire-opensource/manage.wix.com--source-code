import { __assign } from "tslib";
import * as types from '../actionTypes/currentUserActionTypes';
import { HEADER_DATA_FETCH_SUCCESS } from '../actions/dataActions';
export var initialState = {
    id: '',
    language: 'en',
    profileImage: '',
    username: '',
    isPro: false,
    hasProAccount: false,
    countryCode: '',
    fullName: {
        first: '',
        last: ''
    }
};
export function currentUser(state, action) {
    if (state === void 0) { state = initialState; }
    switch (action.type) {
        case HEADER_DATA_FETCH_SUCCESS:
            return action.payload.user ? __assign(__assign({}, state), action.payload.user) : state;
        case types.FETCH_CURRENT_USER_SUCCESS:
            return __assign(__assign({}, state), action.data);
        default:
            return state;
    }
}
//# sourceMappingURL=currentUserReducers.js.map