export var getUserId = function (state) { return state.currentUser.id; };
var getFullName = function (state) {
    var fullName = state.currentUser.fullName;
    if (!fullName) {
        return;
    }
    var first = fullName.first || '';
    var last = fullName.last || '';
    var space = first && last ? ' ' : '';
    return "" + first + space + last;
};
export var isUserDetailsExist = function (state) {
    return !!getUserId(state);
};
export var getDisplayName = function (state) {
    return getFullName(state) || state.currentUser.email || '';
};
export var getCountryCode = function (state) { return state.currentUser.countryCode; };
export var getProfileImage = function (state) {
    var currentUser = state.currentUser || {};
    return currentUser.profileImage;
};
export var getLanguage = function (state) {
    var currentUser = state.currentUser || {};
    return currentUser.language;
};
//# sourceMappingURL=currentUserSelectors.js.map