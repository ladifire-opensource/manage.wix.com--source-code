import { HEADER_DATA_FETCH_SUCCESS } from '../actions/dataActions';
var initialState = null;
export var account = function (state, action) {
    if (state === void 0) { state = initialState; }
    switch (action.type) {
        case HEADER_DATA_FETCH_SUCCESS:
            return action.payload.account || state;
        default:
            return state;
    }
};
//# sourceMappingURL=accountReducer.js.map