import * as React from 'react';
import { connect } from 'react-redux';
import * as css from './Logo.scss';
import { isLogoTypeDetermined, LogoTypes } from '../../selectors/logoSelector';
import CoBrandingImage from './CoBrandingImage';
import LogoItem from './LogoItem';
var Logo = function (_a) {
    var showLogo = _a.showLogo;
    return (React.createElement("div", { "data-hook": "logo-container", className: css.container },
        !showLogo && React.createElement(LogoPlaceholder, null),
        showLogo && React.createElement(React.Fragment, null,
            React.createElement(LogoItem, { showWhenType: LogoTypes.WIX }),
            React.createElement(LogoItem, { showWhenType: LogoTypes.NTT }),
            React.createElement(LogoItem, { showWhenType: LogoTypes.CO_BRANDING },
                React.createElement(CoBrandingImage, null)),
            React.createElement(LogoItem, { showWhenType: LogoTypes.EDITORX }))));
};
var LogoPlaceholder = function () { return React.createElement("div", { className: css.placeholder, "data-hook": "logo-placeholder" }); };
var mapStateToProps = function (state) { return ({
    showLogo: isLogoTypeDetermined(state),
}); };
export default connect(mapStateToProps)(Logo);
//# sourceMappingURL=Logo.js.map