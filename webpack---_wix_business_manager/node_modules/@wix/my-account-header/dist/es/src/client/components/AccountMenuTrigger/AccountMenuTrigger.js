import * as React from 'react';
import { TextButton } from 'wix-style-react';
import { connect } from 'react-redux';
import ChevronDown from 'wix-ui-icons-common/ChevronDown';
import * as css from './AccountMenuTrigger.scss';
import UserProfilePicture from '../UserProfilePicture/UserProfilePicture.container';
import { getDisplayName } from '../../selectors/currentUserSelectors';
export var DISPLAY_NAME_LENGTH = 16;
var textWithEllipsis = function (text) {
    if (text.length > DISPLAY_NAME_LENGTH) {
        return text.substring(0, DISPLAY_NAME_LENGTH) + "\u2026";
    }
    return text;
};
export var AccountMenuTriggerComponent = function (props) { return (React.createElement(TextButton, { as: "div", dataHook: "account-level-display", theme: "no-border" },
    React.createElement("div", { className: css.wrapper + " " + css.wrapperSmallScreen },
        React.createElement("div", { className: css.accountMenuTrigger + " " + css.triggerSmallScreen },
            React.createElement(UserProfilePicture, null)),
        React.createElement("div", { className: css.displayName },
            React.createElement("span", { "data-hook": "display-name" }, textWithEllipsis(props.displayName))),
        React.createElement("div", { className: css.arrowDown },
            React.createElement(ChevronDown, { size: "24px" }))))); };
function mapStateToProps(state) {
    return {
        displayName: getDisplayName(state),
    };
}
export var AccountMenuTrigger = connect(mapStateToProps)(AccountMenuTriggerComponent);
//# sourceMappingURL=AccountMenuTrigger.js.map