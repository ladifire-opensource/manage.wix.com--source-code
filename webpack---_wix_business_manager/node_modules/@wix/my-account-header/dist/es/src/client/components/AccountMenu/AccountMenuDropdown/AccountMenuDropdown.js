import * as React from 'react';
import { connect } from 'react-redux';
import AccountMenu from '../AccountMenu';
import DropdownDialogBi from '../../DropdownDialogBi/DropdownDialogBi.container';
import { AccountMenuTrigger } from '../../AccountMenuTrigger/AccountMenuTrigger';
import { withErrorBoundary } from '@wix/business-manager-common-utils';
import { AccountMenuTeamTrigger } from '../../AccountMenuTeamTrigger/AccountMenuTeamTrigger';
import { getAccountName } from '../../../selectors/accountSelectors';
import { ReactDropdownDialog } from '../../ReactDropdownDialog/ReactDropdownDialog';
export var AccountMenuDropdownComponent = function (_a) {
    var biPageView = _a.biPageView, showTeamMenuTrigger = _a.showTeamMenuTrigger;
    return (React.createElement("div", { "data-hook": "account-menu" },
        React.createElement(ReactDropdownDialog, { openStrategy: 'click', appendToBody: false, narrow: true },
            showTeamMenuTrigger ? React.createElement(AccountMenuTeamTrigger, null) : React.createElement(AccountMenuTrigger, null),
            React.createElement(DropdownDialogBi, { extraBiParams: { view: 'account', page_view: biPageView }, narrow: true },
                React.createElement(AccountMenu, null)))));
};
var mapStateToProps = function (state) {
    return {
        biPageView: state.biPageView,
        showTeamMenuTrigger: !!getAccountName(state),
    };
};
export var AccountMenuDropdown = withErrorBoundary()(connect(mapStateToProps)(AccountMenuDropdownComponent));
//# sourceMappingURL=AccountMenuDropdown.js.map