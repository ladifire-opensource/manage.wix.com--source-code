import { __extends } from "tslib";
import * as React from 'react';
import { Badge } from 'wix-style-react';
import * as css from './HeaderLink.scss';
var HeaderLinkBadge = /** @class */ (function (_super) {
    __extends(HeaderLinkBadge, _super);
    function HeaderLinkBadge() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    HeaderLinkBadge.prototype.render = function () {
        var text = this.props.text;
        return (React.createElement("span", { className: css.badge },
            React.createElement(EightPixelSpace, null),
            React.createElement(Badge, { dataHook: "newFeatureBadge", size: "small", skin: "warning", type: "solid", uppercase: true }, text)));
    };
    return HeaderLinkBadge;
}(React.Component));
export default HeaderLinkBadge;
// The reason there's a weird spacer like this is because we want that when the link breaks into 2 lines
// and the badge goes to the second line - in that scenario we don't want this space, therefore with the advantage
// of spaces not being counted in inline elements when moving to a new line we use this sophisticated code
var EightPixelSpace = function () { return React.createElement("span", { style: { fontSize: '16px' } }, "\u00A0\u00A0"); };
//# sourceMappingURL=HeaderLinkBadge.js.map