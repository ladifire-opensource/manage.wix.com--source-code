import * as React from 'react';
import * as css from './DropdownMenu.scss';
import { ReactDropdownDialog } from '../ReactDropdownDialog/ReactDropdownDialog';
import { DropdownIndicator } from '../HeaderLink/DropdownIndicator';
import DropdownDialogBi from '../DropdownDialogBi/DropdownDialogBi.container';
import { getBiPageView } from '../../selectors/biPageViewSelectors';
import { connect } from 'react-redux';
import classNames from 'classnames';
import { Typography as typ } from 'wix-style-react';
var DropdownComponent = function (_a) {
    var text = _a.text, children = _a.children, biPageView = _a.biPageView, view = _a.view, _b = _a.name, name = _b === void 0 ? '' : _b;
    return (React.createElement(ReactDropdownDialog, { appendToBody: false, openStrategy: 'click', classNames: { dialogComponent: css.dialogComponent } },
        React.createElement("div", { "data-hook": name, className: css.headerLink + " " + css.sitesDropDownSpec },
            text,
            React.createElement(DropdownIndicator, null)),
        React.createElement(DropdownDialogBi, { extraBiParams: { view: view, page_view: biPageView } }, children)));
};
export var Dropdown = connect(function (state) { return ({
    biPageView: getBiPageView(state),
}); })(DropdownComponent);
export var dropdownLinkClasses = classNames(css.link, typ.text, typ.sizeSmall, typ.weightNormal);
//# sourceMappingURL=DropdownMenu.js.map