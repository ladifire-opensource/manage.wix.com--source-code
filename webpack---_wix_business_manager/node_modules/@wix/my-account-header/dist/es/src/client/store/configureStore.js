import { createStore, applyMiddleware } from 'redux';
import thunk from 'redux-thunk';
import { createLogger } from 'redux-logger';
import { rootReducer } from '../reducers';
export function configureStore(initialState, services) {
    var middleware = [thunk.withExtraArgument(services)];
    if (initialState.isDebug) {
        middleware.push(createLogger());
    }
    return createStore(rootReducer, initialState, applyMiddleware.apply(void 0, middleware));
}
//# sourceMappingURL=configureStore.js.map