export default function getLanguageOrderFunction(length) {
    var half = length % 2 ? length / 2 - 0.5 : length / 2 - 1;
    return function (index) {
        return half >= index ? index * 2 : (index - half) * 2 - 1;
    };
}
/* What is this ?
 assuming we have this array:
 [0,1,2,3,4,5]
 and we want to order them in rows, each row contain 2 items, they will look like this:
 0 1
 2 3
 4 5

 We want to reorder them so they will be ordered like this in columns:
 0 3
 1 4
 2 5

 Therefore this service takes an index in a list and returns the correct order for it, like this:
 Item No.0 --> order No. 0
 Item No.1 --> order No. 2
 Item No.2 --> order No. 4
 Item No.3 --> order No. 1
 Item No.4 --> order No. 3
 Item No.5 --> order No. 5
 */
//# sourceMappingURL=LanguagesListOrder.js.map