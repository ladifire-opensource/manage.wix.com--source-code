import { getSiteCoBrandingLogo, getSiteCoBrandingType, siteCoBrandingDataExists } from './siteCoBrandingSelector';
import { getAccountCoBrandingLogo, getAccountCoBrandingType, isAccountCoBrandingDataReceived, } from './accountSelectors';
export function getCoBrandingType(state) {
    return getSiteCoBrandingType(state) || getAccountCoBrandingType(state);
}
export function isCoBranded(state) {
    return !!getCoBrandingType(state);
}
export function isNotCoBranded(state) {
    if (siteCoBrandingDataExists(state)) {
        return !state.siteCoBranding.loggedInAccountBranding;
    }
    else {
        return isAccountCoBrandingDataReceived(state) && (!state.account || !state.account.coBranding);
    }
}
export function getCoBrandingLogoUrl(state) {
    if (!isCoBranded(state)) {
        return '';
    }
    return getSiteCoBrandingLogo(state) || getAccountCoBrandingLogo(state);
}
//# sourceMappingURL=coBrandingSelector.js.map