import * as React from 'react';
import { connect } from 'react-redux';
import * as css from './Logo.scss';
import classnames from 'classnames';
import { getLogoType } from '../../selectors/logoSelector';
var LogoItem = function (_a) {
    var biPageView = _a.biPageView, children = _a.children, customClassName = _a.customClassName, dataHook = _a.dataHook, show = _a.show;
    if (!show) {
        return null;
    }
    var classNames = classnames(css.logoLink, customClassName, css.sitesDropDown);
    return (React.createElement("div", { className: classNames, "data-hook": dataHook },
        React.createElement("a", { href: "/?referralAdditionalInfo=" + biPageView }, children)));
};
var mapStateToProps = function (state, ownProps) {
    var currentLogoType = getLogoType(state);
    return ({
        biPageView: state.biPageView,
        customClassName: css[currentLogoType],
        dataHook: "logo-link-" + currentLogoType,
        show: currentLogoType === ownProps.showWhenType,
    });
};
export default connect(mapStateToProps)(LogoItem);
//# sourceMappingURL=LogoItem.js.map