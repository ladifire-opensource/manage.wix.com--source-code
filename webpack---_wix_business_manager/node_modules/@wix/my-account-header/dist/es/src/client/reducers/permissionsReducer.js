import { HEADER_DATA_FETCH_SUCCESS } from '../actions/dataActions';
var initialState = [];
export var permissions = function (state, action) {
    if (state === void 0) { state = initialState; }
    switch (action.type) {
        case HEADER_DATA_FETCH_SUCCESS:
            return action.payload.permissions || state;
        default:
            return state;
    }
};
//# sourceMappingURL=permissionsReducer.js.map