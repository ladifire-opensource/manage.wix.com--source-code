import { connect } from 'react-redux';
import { getBiPageView } from '../../selectors/biPageViewSelectors';
import { getCurrentPage } from '../../selectors/currentPageSelectors';
import { HeaderLink } from './HeaderLink';
import { provideLogger } from '../../services/BiLogger';
import { getCurrentSiteId } from '../../selectors/currentSiteSelectors';
var mapStateToProps = function (state) { return ({
    biPageView: getBiPageView(state),
    currentPage: getCurrentPage(state),
    msid: getCurrentSiteId(state),
}); };
export default connect(mapStateToProps)(provideLogger(HeaderLink));
//# sourceMappingURL=HeaderLink.container.js.map