import { __assign } from "tslib";
import { HEADER_DATA_FETCH_SUCCESS } from '../actions/dataActions';
export var currentSite = function (state, action) {
    if (state === void 0) { state = {}; }
    var type = action.type, payload = action.payload;
    switch (type) {
        case HEADER_DATA_FETCH_SUCCESS:
            if (payload.siteName) {
                return __assign(__assign({}, state), { siteName: action.payload.siteName });
            }
            else {
                return state;
            }
        default:
            return state;
    }
};
//# sourceMappingURL=currentSiteReducer.js.map