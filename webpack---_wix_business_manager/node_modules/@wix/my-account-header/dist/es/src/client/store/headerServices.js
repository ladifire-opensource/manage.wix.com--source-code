import { create } from '@wix/fedops-logger';
import { APP_NAME } from '../services/fedops-interactions';
import { createBiLogger } from '../services/BiLogger';
export var buildServices = function () {
    var fedopsLogger = create(APP_NAME);
    var biLogger = createBiLogger();
    return { fedopsLogger: fedopsLogger, biLogger: biLogger };
};
//# sourceMappingURL=headerServices.js.map