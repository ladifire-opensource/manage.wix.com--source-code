import { isUserDetailsExist } from './currentUserSelectors';
export function hasAccount(state) {
    return !!state.account;
}
export function getAccountId(state) {
    return hasAccount(state) && state.account.accountId;
}
export function getAccountName(state) {
    return hasAccount(state) && state.account.accountName;
}
export function getAccountImage(state) {
    return hasAccount(state) && state.account.accountImg;
}
export function getAccountCoBrandingType(state) {
    return hasAccount(state) && state.account.coBranding;
}
export function getAccountCoBrandingLogo(state) {
    return hasAccount(state) && state.account.accountLogo;
}
export function isAccountCoBrandingDataReceived(state) {
    return isUserDetailsExist(state);
}
export function isAccountNotBranded(state) {
    return isAccountCoBrandingDataReceived(state) && !getAccountCoBrandingType(state);
}
//# sourceMappingURL=accountSelectors.js.map