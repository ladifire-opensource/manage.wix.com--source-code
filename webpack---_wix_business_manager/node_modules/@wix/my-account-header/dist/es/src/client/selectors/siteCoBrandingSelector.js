export function siteCoBrandingDataExists(state) {
    return !!state.siteCoBranding;
}
export function getSiteCoBrandingData(state) {
    var data = siteCoBrandingDataExists(state) && state.siteCoBranding.siteOwnerCoBranding;
    return data || {};
}
export function getSiteCoBrandingLogo(state) {
    var accountLogo = getSiteCoBrandingData(state).accountLogo;
    return accountLogo;
}
// This function checks that we got the siteCoBranding data and we know from that data that it's not coBranded
// as appose to: we didn't get the siteCoBranding so we don't know for sure what's the siteCoBranding status and we'll take that status from the accountCoBranding
export function isSiteNotBranded(state) {
    return siteCoBrandingDataExists(state) && !state.siteCoBranding.loggedInAccountBranding;
}
export function getSiteCoBrandingType(state) {
    return siteCoBrandingDataExists(state) && state.siteCoBranding.loggedInAccountBranding;
}
//# sourceMappingURL=siteCoBrandingSelector.js.map