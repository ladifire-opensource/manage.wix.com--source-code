import * as Sentry from '@sentry/browser';
var sentryClient;
export var getSentryClient = function () {
    if (!sentryClient) {
        try {
            sentryClient = new Sentry.BrowserClient({
                dsn: 'https://e9fd3b35ea024000b1cad69eee2e4592@sentry.wixpress.com/1695'
            });
        }
        catch (e) {
            //ignore
        }
    }
    return sentryClient;
};
//# sourceMappingURL=sentry-client.js.map