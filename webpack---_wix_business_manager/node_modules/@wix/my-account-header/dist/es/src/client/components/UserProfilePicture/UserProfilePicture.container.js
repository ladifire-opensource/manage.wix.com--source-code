import { connect } from 'react-redux';
import { UserProfilePicture } from './UserProfilePicture';
import { getProfileImage } from '../../selectors/currentUserSelectors';
function mapStateToProps(state) {
    return {
        profileImage: getProfileImage(state),
    };
}
export default connect(mapStateToProps)(UserProfilePicture);
//# sourceMappingURL=UserProfilePicture.container.js.map