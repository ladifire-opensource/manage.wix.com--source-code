import biLogger from '@wix/web-bi-logger';
import * as URI from 'urijs';
var bi = biLogger
    .factory({ endpoint: 'dash' })
    .updateDefaults({ src: 5 })
    .logger();
var eventMap = {
    CLICK_VISIT_HELP_CENTER: 44,
    CHAT_TOOLTIP_SHOW: 583,
    CHAT_TOOLTIP_CLICK: 584,
    CALLBACK_EXPOSURE: 149
};
export var logClickOnVisitHelpCenter = function (_a) {
    var origin = _a.origin, msid = _a.msid, page_view = _a.page_view;
    bi.log({
        evid: eventMap.CLICK_VISIT_HELP_CENTER,
        origin: origin,
        page_view: page_view,
        msid: !!msid ? msid : null,
    });
};
export var logCallbackRequestExposure = function (_a) {
    var msid = _a.msid;
    var hosting = new URI().search(true).viewMode || 'MA';
    bi.log({
        evid: eventMap.CALLBACK_EXPOSURE,
        hosting: hosting,
        exposure_name: 'displayContactUsLinkInHelpWidget',
        msid: msid
    });
};
//# sourceMappingURL=bi-logger.js.map