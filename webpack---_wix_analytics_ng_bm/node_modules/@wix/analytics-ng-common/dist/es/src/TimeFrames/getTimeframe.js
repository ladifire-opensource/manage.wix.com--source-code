import { PredefinedTimeframe } from '@wix/ambassador-analytics-ng-server/types';
import { DaysTimeFrame } from './DaysTimeFrame';
import { ThisWeekTimeframe } from './ThisWeekTimeframe';
import { ThisMonthTimeframe } from './ThisMonthTimeframe';
export function getTimeFrame(timeFrame) {
    var timeFrameObj;
    switch (timeFrame) {
        case PredefinedTimeframe.LAST_7_DAYS:
            timeFrameObj = new DaysTimeFrame(7);
            break;
        case PredefinedTimeframe.LAST_14_DAYS:
            timeFrameObj = new DaysTimeFrame(14);
            break;
        case PredefinedTimeframe.LAST_30_DAYS:
            timeFrameObj = new DaysTimeFrame(30);
            break;
        case PredefinedTimeframe.LAST_90_DAYS:
            timeFrameObj = new DaysTimeFrame(90);
            break;
        case PredefinedTimeframe.THIS_WEEK:
            timeFrameObj = new ThisWeekTimeframe();
            break;
        case PredefinedTimeframe.THIS_MONTH:
            timeFrameObj = new ThisMonthTimeframe();
            break;
        default:
            throw new Error("TimeFrame.getTimeFrame - Timeframe not supported: '" + timeFrame + "'");
    }
    return timeFrameObj;
}
//# sourceMappingURL=getTimeframe.js.map