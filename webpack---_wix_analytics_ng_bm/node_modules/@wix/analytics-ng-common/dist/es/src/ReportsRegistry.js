import { reports, embeddedWidgets, requirements } from './reports';
var ReportsRegistry = /** @class */ (function () {
    function ReportsRegistry() {
    }
    // Should I clone all responses?
    ReportsRegistry.getReportData = function (id) {
        return reports.find(function (report) { return report.reportId === id; });
    };
    ReportsRegistry.getQuery = function (id) {
        // TODO: implement
        return null;
    };
    ReportsRegistry.getUnifiedApi = function (id) {
        // TODO: implement
        return null;
    };
    ReportsRegistry.getDashboardReports = function () {
        return reports.filter(function (report) { return report.isDashboardReport; });
    };
    ReportsRegistry.getWidgetData = function (widgetId) {
        return embeddedWidgets[widgetId];
    };
    ReportsRegistry.getRequirement = function (requirementId) {
        return requirements[requirementId];
    };
    ReportsRegistry.getActions = function (widgetId, action) {
        var widget = ReportsRegistry.getWidgetData(widgetId);
        return widget.actions.filter(function (actionData) { return actionData.action === action; });
    };
    return ReportsRegistry;
}());
export { ReportsRegistry };
//# sourceMappingURL=ReportsRegistry.js.map