import { __spreadArrays } from "tslib";
export function chunk(arr, size) {
    return Array.from({ length: Math.ceil(arr.length / size) }, function (_value, i) {
        return arr.slice(i * size, i * size + size);
    });
}
export function chunkRight(arr, size) {
    var rm = arr.length % size;
    return rm ? __spreadArrays([arr.slice(0, rm)], chunk(arr.slice(rm), size)) : chunk(arr, size);
}
//# sourceMappingURL=utils.js.map