import { useContext } from 'react';
import { assertEnabledApi } from '../util/assert-enabled-api';
import { SentryContext } from './SentryProvider';
var useSentry = function () {
    var sentry = useContext(SentryContext);
    assertEnabledApi(sentry, 'sentry');
    return sentry;
};
export default useSentry;
//# sourceMappingURL=useSentry.js.map