export function assertErrorMonitorEnabledApi(value, isErrorMonitorEnabled, apiName) {
    if (process.env.NODE_ENV !== 'production') {
        if (value == null) {
            if (apiName === 'sentry' && isErrorMonitorEnabled) {
                throw new Error("Invalid access to flowAPI." + apiName + " - you cannot use Sentry when \"sentry.errorMonitor\" is configured \"true\". Details: https://bo.wix.com/pages/yoshi/docs/business-manager-flow/runtime-api/error-monitor");
            }
            else if ((apiName === 'sentry' && !isErrorMonitorEnabled) ||
                (apiName !== 'sentry' && isErrorMonitorEnabled)) {
                throw new Error("Invalid access to flowAPI." + apiName + " - must be enabled via flow configurations");
            }
        }
    }
}
//# sourceMappingURL=assert-error-monitor-enabled-api.js.map